spring:
  application:
    name: taxi-user
  cloud:
    config:
      uri: http://peer1:9998
      fail-fast: true
logging:
  level: debug
  path: ./log/taxi-user
  config: classpath:logback-spring-${spring.profiles.active}.xml
---
spring:
  profiles:
    active: local