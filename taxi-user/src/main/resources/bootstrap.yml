spring:
  application:
    name: taxi-user
  cloud:
    config:
      uri: http://127.0.0.1:9998
      fail-fast: true
logging:
  level: debug
  tk.mybatis.mapper: debug
---
spring:
  profiles:
    active: local