spring:
  application:
    name: taxi-user
  cloud:
    config:
      uri: http://127.0.0.1:9998
      fail-fast: true
  aop:
    auto: true
    proxy-target-class: true
    output:
      ansi:
        enabled: ALWAYS
---
spring:
  profiles:
    active: local