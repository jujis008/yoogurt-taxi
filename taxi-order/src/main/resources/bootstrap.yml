spring:
  application:
    name: taxi-order
  cloud:
    config:
      uri: http://peer1:9998
      fail-fast: true
logging:
  level: debug
  tk.mybatis.mapper: debug
---
spring:
  profiles:
    active: local