spring:
  application:
    name: taxi-order
  cloud:
    config:
      uri: http://peer1:9998
      fail-fast: true
logging:
  level: debug
  path: ./log/taxi-order
  config: classpath:logback-spring-${spring.profiles.active}.xml
---
spring:
  profiles:
    active: local